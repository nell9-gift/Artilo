@extends('artisan.layout', ['pageActive' => 'dashboard', 'pageTitle' => 'Tableau de bord', 'pageSubtitle' => 'Suivez vos opportunités et interventions.'])

@section('content')
    <div style="display:flex;justify-content:space-between;gap:16px;align-items:center;margin-bottom:22px"><div><h1 style="margin:0;font-size:24px">Bonjour {{ auth()->user()->name }}</h1><p style="color:var(--muted);margin:6px 0 0">{{ $artisan->profession ?: 'Votre activité' }}@if($artisan->intervention_area) · {{ $artisan->intervention_area }}@endif</p></div><a href="{{ route('artisan.missions.index') }}" class="pro-button">Voir mes missions</a></div>
    <div class="pro-stat-grid" style="margin-bottom:22px"><div class="pro-card pro-stat"><b>{{ $stats['total'] }}</b><span>Missions attribuées</span></div><div class="pro-card pro-stat"><b>{{ $stats['pending'] }}</b><span>À répondre</span></div><div class="pro-card pro-stat"><b>{{ $stats['active'] }}</b><span>En cours</span></div><div class="pro-card pro-stat"><b>{{ $stats['completed'] }}</b><span>Terminées</span></div></div>
    <section class="pro-card" style="padding:22px"><div style="display:flex;justify-content:space-between;align-items:center"><h2 style="font-size:18px;margin:0">Dernières missions</h2><a class="pro-button secondary" href="{{ route('artisan.missions.index') }}">Toutes les missions</a></div>@forelse($missions->take(5) as $mission)<article class="pro-mission"><div><h3>{{ ucfirst($mission->metier_requis) }}</h3><p>{{ $mission->particulier?->name ?? 'Client' }} · {{ $mission->adresse }}</p></div><div><span class="pro-badge">{{ $mission->statut_libelle }}</span></div></article>@empty<div style="padding-top:18px;color:var(--muted)">Aucune mission ne vous a encore été attribuée.</div>@endforelse</section>
@endsection
