@extends('artisan.layout', ['pageActive' => 'missions', 'pageTitle' => 'Détail de la mission', 'pageSubtitle' => 'Informations et actions disponibles.'])

@section('content')
    <section class="pro-card" style="padding:24px"><div style="display:flex;justify-content:space-between;gap:15px;align-items:flex-start"><div><h1 style="margin:0;font-size:23px">{{ ucfirst($mission->metier_requis) }}</h1><p style="color:var(--muted)">{{ $mission->adresse }}</p></div><span class="pro-badge">{{ $mission->statut_libelle }}</span></div><hr style="border:0;border-top:1px solid var(--line);margin:20px 0"><p style="white-space:pre-line;line-height:1.6">{{ $mission->description }}</p>@if($mission->date_souhaitee)<p><strong>Date souhaitée :</strong> {{ $mission->date_souhaitee->format('d/m/Y') }}</p>@endif @if($mission->statut === 'affectee')<div style="display:flex;gap:10px;margin-top:20px"><form method="POST" action="{{ route('artisan.missions.accepter', $mission) }}">@csrf<button class="pro-button" type="submit">Accepter</button></form><form method="POST" action="{{ route('artisan.missions.refuser', $mission) }}">@csrf<button class="pro-button secondary" type="submit">Refuser</button></form></div>@endif</section>
@endsection
